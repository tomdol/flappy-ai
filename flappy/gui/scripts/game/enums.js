const VerticalPositioning = Object.freeze({
    CENTER_POINT: 1,
    TOP_POINT: 2
});

const NoseDirection = Object.freeze({
    CLIMBING: -30,
    CLIMBING_LIGHT: -15,
    STRAIGHT_FLIGHT: 0,
    FALLING: 30,
    FALLING_FASTER: 45,
    ALMOST_DIVING: 55,
    DIVING: 70
});
